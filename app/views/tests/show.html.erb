<h1><%= @test.title %></h1>

<table>
  <tr>
    <th>Questions</th>
    <th colspan="3">Actions</th>
  </tr>

  <%= render @test.questions %>
</table>

<p>
  <%= link_to 'Add new question', new_test_question_path(@test) %> | 
  <%= link_to 'Back to test list', tests_path %>
</p>
