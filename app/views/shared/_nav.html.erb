<span>
  <%= link_to 'TestGuru', root_path %>
</span>

<% if user_signed_in? %>
  <span class="nav user">Welcome, <b><%= current_user.email %></b> Guru |
    <%= link_to 'Logout', destroy_user_session_path, method: 'delete' %>
  </span>
<% end %>
